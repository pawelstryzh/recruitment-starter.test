<header class="header">
  <div class="container">
    <a class="header__logo" href="{{ home_url('/') }}">
      <img src="{!! get_field('logo', 'option')['url'] !!}"
           alt="{{ get_bloginfo('name', 'display') }}">
    </a>
  </div>
</header>
